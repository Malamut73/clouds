server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Malamut73/springcloud-config.git
          search-paths: step07
          clone-on-start: true
  application:
    name: config_server

logging:
  level:
    org.springframework.cloud: DEBUG